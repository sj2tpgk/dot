run =./install.sh home ~= (and =./install.sh home-g ~= when GUI) to install

=tmux= is a shell script that is a self contained minimal config (shell, tmux, vim, some scripts ...)

=tmux2= is similar but has more shell scripts. (generated by =./tmux pack=)
